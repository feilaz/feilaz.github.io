import type {Metadata} from 'next';
import Link from 'next/link';
import {roles,education,awards,skills} from '@/content/experience';
import {Arrow} from '@/components/v2/Chrome';
export const metadata:Metadata={title:'About',description:'Adam Kostka: AI engineer at Strategy, computer science researcher, and builder of intelligent systems.',alternates:{canonical:'/about/'}};
export default function About(){return <div className="container page-content"><header className="page-heading about-heading"><p className="eyebrow">ABOUT / ADAM KOSTKA</p><h1>An engineer’s instinct.<br/><span>A researcher’s questions.</span></h1><div className="about-intro"><div className="location-block"><span className="location-mark" aria-hidden="true">↗</span><p>Warsaw, Poland</p><span>52.23° N · 21.01° E</span></div><div><p>I work on intelligent systems from both sides: building the software that puts agents to work, and studying the assumptions that make those systems reliable—or fragile.</p><p>At Strategy, that means shipping LLM-agent features and customer-facing software. In research at Warsaw University of Technology, it means asking when several models agreeing should actually increase our confidence.</p><div className="inline-links"><a className="text-link" href="mailto:adamkostka002@gmail.com">Get in touch <Arrow diagonal/></a><a className="text-link" href="/adam-kostka-cv.pdf">Download CV <Arrow diagonal/></a></div></div></div></header><section className="bio-section"><div><p className="eyebrow">01 / EXPERIENCE</p><h2>Built in practice.</h2></div><div className="timeline">{roles.map((r,i)=><article key={r.org}><div className="timeline-date">{r.start} — {r.end}</div><h3>{r.org}</h3><p className="role-title">{r.title}</p><p>{i===0?'Delivering agentic AI features across Python and OpenAPI services. Owned the end-to-end delivery of a Gantt visualization, and building an internal enterprise learning platform.':i===1?'Co-founded a two-person AI venture. Built and deployed HermesFix on AWS, and prototyped tools for controlled agent execution and learning from code.':'Tested banking-platform APIs and workflows, reproduced defects, and documented findings with SoapUI and Jira.'}</p><div className="topic-tags">{r.stack?.map(s=><span key={s}>{s}</span>)}</div></article>)}</div></section><section className="bio-section"><div><p className="eyebrow">02 / EDUCATION</p><h2>A wider perspective.</h2></div><div className="education-list">{education.map(e=><article key={e.degree+e.institution}><p className="timeline-date">{e.start} — {e.end}</p><h3>{e.institution}</h3><p>{e.degree}</p><span>{e.detail}</span></article>)}<div className="awards"><h3>Recognition</h3>{awards.map(a=><p key={a.title}><strong>{a.title}</strong><span>{a.detail||a.year}</span></p>)}</div></div></section><section className="bio-section"><div><p className="eyebrow">03 / TOOLKIT</p><h2>Tools follow the problem.</h2></div><div className="skills-list">{skills.filter(s=>s.group!=='Additional'&&s.group!=='Languages').map(s=><div key={s.group}><h3>{s.group}</h3><p>{s.items.join(' · ')}</p></div>)}<Link className="text-link" href="/#work">See them in the work <Arrow/></Link></div></section></div>}
